{
  "name": "monteur-ia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync.mjs --style-only",
    "dev": "hyperframes preview",
    "precheck": "node scripts/sync.mjs --style-only",
    "check": "hyperframes lint && hyperframes validate && hyperframes inspect",
    "prerender": "node scripts/sync.mjs --style-only",
    "render": "hyperframes render",
    "sync": "node scripts/sync.mjs",
    "postinstall": "node scripts/sync.mjs || true"
  },
  "devDependencies": {
    "hyperframes": "0.6.72"
  }
}
