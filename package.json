{
  "name": "monteur-ia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "hyperframes preview",
    "check": "hyperframes lint && hyperframes validate && hyperframes inspect",
    "render": "hyperframes render",
    "sync": "node scripts/sync.mjs",
    "postinstall": "node scripts/sync.mjs || true"
  },
  "devDependencies": {
    "hyperframes": "0.6.72"
  }
}
