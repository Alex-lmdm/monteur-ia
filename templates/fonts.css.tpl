/* ==========================================================================
   Design system — Polices (HyperFrames)

   ⚠️ FICHIER GÉNÉRÉ. Ne pas éditer : il est réécrit à chaque `npm run sync`.
   Source : templates/fonts.css.tpl + brand.config.json (bloc `visual`)
            + la table `fonts` de templates/style-presets.json.

   Toutes les polices sont hébergées EN LOCAL (assets/fonts/) : le rendu doit être
   déterministe et marcher hors-ligne. JAMAIS de @import Google Fonts.

   Ajouter TA police : dépose les fichiers dans assets/fonts/, déclare-les dans la
   table `fonts` de templates/style-presets.json, puis `npm run sync`.
   ========================================================================== */

{{FONT_FACES}}
